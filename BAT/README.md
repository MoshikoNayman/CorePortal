# BAT (Deploy marker)

BAT is implemented in `deploy/coreportal/coreportal.py` and served at `/BAT`.

This folder is a marker so the deploy structure is explicit.

Current BAT page includes:

- ledger and cashflow tools
- recurring salary/spending setup
- built-in owner overview (balances + net worth)
