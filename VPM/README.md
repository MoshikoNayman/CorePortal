# VPM (Virtual Portfolio Manager)

This folder is reserved for Virtual Portfolio Manager specific assets and future modular code.

Current runtime entrypoint:

- `../coreportal.py`

CorePortal serves VPM at `/VPM`.

VPM-owned data lives beside this app directory:

- `virtual_portfolio.db`
- `portfolio_backups/`
